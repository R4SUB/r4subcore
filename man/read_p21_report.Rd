% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_p21_report.R
\name{read_p21_report}
\alias{read_p21_report}
\title{Read a Pinnacle 21 Report from Disk}
\usage{
read_p21_report(path, format = c("auto", "csv", "xlsx"), sheet = NULL)
}
\arguments{
\item{path}{Path to the report file.}

\item{format}{One of \code{"auto"} (infer from the file extension), \code{"csv"}, or
\code{"xlsx"}. Default \code{"auto"}.}

\item{sheet}{For Excel reports, the sheet to read. When \code{NULL} (the default),
a sheet whose name looks like the findings list (matching "detail", "issue",
or "finding") is used, otherwise the first sheet.}
}
\value{
A data.frame of the report rows with the original column names.
}
\description{
Loads a Pinnacle 21 validation report (the export most teams already produce
for every submission) into a data.frame, ready for \code{\link[=p21_to_evidence]{p21_to_evidence()}}. CSV
exports are read with base R. Excel exports are read with the \code{readxl}
package when it is installed; if it is not, export the report as CSV instead.
}
\details{
This function only reads the file; it does not interpret the columns. Pass the
result to \code{\link[=p21_to_evidence]{p21_to_evidence()}}, which detects the rule, message, severity,
dataset, variable, and result columns case-insensitively.
}
\examples{
# Simulate a small CSV export and read it back.
p21 <- data.frame(
  "Rule ID" = c("SD0001", "SD0002"),
  Message   = c("Missing variable label", "Invalid format"),
  Severity  = c("Error", "Warning"),
  Dataset   = c("ADSL", "ADAE"),
  Variable  = c("AGEU", "AESEQ"),
  Status    = c("Failed", "Warning"),
  check.names = FALSE
)
csv <- tempfile(fileext = ".csv")
utils::write.csv(p21, csv, row.names = FALSE)
report <- read_p21_report(csv)
report

}
\seealso{
\code{\link[=p21_to_evidence]{p21_to_evidence()}} to convert the result into evidence.
}
