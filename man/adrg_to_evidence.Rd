% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsers_adrg.R
\name{adrg_to_evidence}
\alias{adrg_to_evidence}
\title{Convert ADRG Section Coverage to Evidence}
\usage{
adrg_to_evidence(
  present,
  ctx,
  source_name = "adrg",
  source_version = NULL,
  standard = adrg_sections()
)
}
\arguments{
\item{present}{A character vector of \code{section_id}s the guide contains, or a
data.frame with a \code{section_id} column and a logical \code{present} column.}

\item{ctx}{An \link{r4sub_run_context}.}

\item{source_name}{Source label. Default \code{"adrg"}; use \code{"sdrg"} for a Study
Data Reviewer's Guide.}

\item{source_version}{Optional source version string.}

\item{standard}{The section catalog to score against. Defaults to
\code{\link[=adrg_sections]{adrg_sections()}}.}
}
\value{
An evidence data.frame, one row per standard section.
}
\description{
Scores an Analysis Data Reviewer's Guide for completeness against the
standard section catalog and emits R4SUB evidence rows, one per section.
}
\details{
ADRG and SDRG documents are Word or PDF, and reliable text extraction from
them is out of scope for this package. \code{adrg_to_evidence()} therefore takes a
structured summary of which sections a guide contains, which a document
pipeline (or a human reviewer) can produce, and turns it into evidence. A
present section passes. A missing required section fails at high severity; a
missing recommended section warns at low severity. The rows land in the
usability pillar, since the reviewer's guide is reviewer-facing documentation.
}
\examples{
ctx <- suppressMessages(r4sub_run_context("STUDY1", "DEV"))
ev <- suppressMessages(adrg_to_evidence(c("1", "2", "3", "4", "5"), ctx))
ev[, c("indicator_id", "severity", "result")]

}
