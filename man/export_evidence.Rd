% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_evidence.R
\name{export_evidence}
\alias{export_evidence}
\title{Export Evidence Table to File}
\usage{
export_evidence(evidence, file, format = c("csv", "rds", "json"))
}
\arguments{
\item{evidence}{A valid evidence data.frame (as produced by \code{\link[=as_evidence]{as_evidence()}}).}

\item{file}{Character. Destination file path (including extension).}

\item{format}{Character. One of \code{"csv"}, \code{"rds"}, or \code{"json"}.
Default: \code{"csv"}.}
}
\value{
Invisibly returns \code{file} with attributes:
\describe{
\item{exported_at}{POSIXct timestamp of export.}
\item{r4subcore_version}{Package version string.}
\item{nrow}{Number of rows written.}
}
}
\description{
Validates the evidence table then writes it to disk in the requested format.
Metadata attributes (\code{exported_at}, \code{r4subcore_version}, \code{nrow}) are
attached to the returned path value for traceability.
}
\examples{
ctx <- suppressMessages(r4sub_run_context("STUDY1", "DEV"))
ev <- suppressMessages(as_evidence(
  data.frame(
    asset_type = "validation", asset_id = "ADSL",
    source_name = "pinnacle21", indicator_id = "SD0001",
    indicator_name = "SD0001", indicator_domain = "quality",
    severity = "high", result = "fail",
    stringsAsFactors = FALSE
  ),
  ctx = ctx
))
tmp_csv <- tempfile(fileext = ".csv")
out <- suppressMessages(export_evidence(ev, tmp_csv, format = "csv"))
file.exists(out)

}
